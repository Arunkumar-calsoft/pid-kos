# LLM API Keys — loaded automatically by agent/cli.py
# Copy this file to groq.env and add your real keys.
# NEVER commit groq.env to source control (it is in .gitignore).

# ── Groq (https://console.groq.com) ─────────────────────────────────────────
GROQ_API_KEY = your_groq_api_key_here

# ── Google Gemini (https://aistudio.google.com/app/apikey) ──────────────────
# Free tier: gemini-2.5-flash / gemini-2.0-flash (1 M tokens/day, no card)
#GEMINI_API_KEY = your_gemini_key_here
