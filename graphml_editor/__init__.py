# graphml_editor — Admin-only GraphML correction tool
#
# STANDALONE — this package is NOT imported by server.py, agent/, or engine/.
# It is only used by editor_server.py (port 8081).
# The main pipeline has zero dependency on this package.
